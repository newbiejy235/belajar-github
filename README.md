## CLI / Perintah github

- git switch -c register : untk membuat branch baru sekaligus pindah ke branch tersebut

-git push -u origin <nama branch> : untuk push ke barnch tertentu

-git merge --abort : membatalkan merge

-git merge <nama branch> atau git pull <nama branch> : buat gabungin isi branch yang berbeda/ mengambiil perubahan

- git switch <nama branch> / git checkout <nama branch> : buat ganti branch

  
